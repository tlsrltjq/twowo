rules_version = '2';
// 통합 테스트 전용 규칙 — 에뮬레이터에서만 사용, 절대 프로덕션에 배포 금지
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
