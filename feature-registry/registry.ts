import type { AppFeature } from './types';

const registry: AppFeature[] = [
  // 1차 MVP 이후 features 가 여기에 등록됩니다.
  // 예: { id: 'mood-share', name: '오늘의 컨디션', description: '...', status: 'experimental' }
];

export default registry;
